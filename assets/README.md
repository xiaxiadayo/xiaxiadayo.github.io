# 素材说明 / Asset Guide

本目录包含网站所需的素材资源。

## 📁 目录结构

```
assets/
├── images/          # 图片资源（可自行替换）
└── README.md        # 本文件
```

## 🎨 图片替换指引

如需使用自定义图片：
1. 将新图片放入 `/assets/images/` 目录
2. 在 `index.html` 中添加对应的 `<img>` 标签
3. 支持 SVG、PNG、JPG、WebP 格式

## 📜 许可证

素材随项目使用，请确保替换的素材符合其许可证要求。
